other:
  gravatar_email: "http://gravatar.com/emails"

validate:
  name:
    max_length: 50
  email:
    max_length: 255
    # regex: !ruby/regexp '/\A[\w+-.]+@[a-z\d-.]+.[a-z]+\z/i'
  password:
    min_length: 6
  post:
    max_length: 140
  course_name:
    max_length: 255

remember_me: "1"

paging: 
  user: "10"
  course: "5"

password_expired_time: 1

